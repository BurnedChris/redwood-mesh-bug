{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web",
      "mesh"
    ]
  },

  "scripts": {
    "mesh": "cd ./mesh && yarn mesh serve"
  },
  "devDependencies": {
    "@redwoodjs/core": "^0.23.0"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config"
  },
  "engines": {
    "node": ">=12",
    "yarn": ">=1.15"
  }
}
